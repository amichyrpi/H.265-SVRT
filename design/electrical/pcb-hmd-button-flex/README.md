# Stearlight HMD front panel button flex

This project implements the headset side-button flex requested for Stearlight.The enclosure-facing side carries three vertically aligned, normally-open buttons:

1. `SW1` — volume up (`+`)
2. `SW2` — power (`PWR`)
3. `SW3` — volume down (`-`)
