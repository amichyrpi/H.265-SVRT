add_library(stearlight_protocol STATIC stearlight_protocol.c)
add_library(stearlight::protocol ALIAS stearlight_protocol)
target_compile_features(stearlight_protocol PUBLIC c_std_11)
target_include_directories(stearlight_protocol PUBLIC include)
if(WIN32)
  target_link_libraries(stearlight_protocol PUBLIC ws2_32)
endif()
if(SVRT_BUILD_TESTS)
  add_executable(stearlight-protocol-test tests/protocol_test.c)
  target_link_libraries(stearlight-protocol-test PRIVATE stearlight::protocol)
  add_test(NAME stearlight-protocol COMMAND stearlight-protocol-test)
endif()
